
1.0.20+18
    • Push notification log: every FCM push (song, chat, read-sync) recorded in HC.PushLog
      with BatchId, QueryType, EventId, RecipientUserId, FcmToken, IsVisible, Summary

1.0.19+17
    • Songbook: disable external push notifications — all song pushes are data-only silent push for now

1.0.18+16
    • Interactive songbook: FCM payload now includes EventId/SongId (fixes real-time update)
    • Visible push notification with sound for users with notifications enabled; data-only silent push for others
    • Checked-in attendees (attendenceState >= 3) added to FCM recipient list alongside RSVP'd users

1.0.17+15
    • Interactive songbook: selectSong queryType fans out a data-only FCM silent push (song_selected) to all RSVP'd attendees via SendSongToAttendeesAsync

1.0.16+14
    • Fix: ProxyGeocode now calls Azure Maps directly using AzureMapsSubscriptionKey — removes Logic App dependency that was returning 400 (POST-only trigger vs GET request)
    • Improvement: ProxyGeocode now accepts lat/lon/countryCodes params for location-biased results
    • Portal: performGazetteerSearch passes kennel lat/lon and country codes to the proxy

1.0.15+13
    • Fix: HC6 error envelope reads errorUserMessage/errorId from rowset 1
      (detail row), not rowset 0 (success envelope) — errorUserMessage was
      always null in 400 responses for auth failures such as approveLogin

1.0.14+12
    • Security: SQL injection fixes — EmailInviteCode and GenericJsonQueryHC6 fully parameterised
    • Security: GetPositions now requires API key — live GPS data no longer publicly accessible
    • Security: PublicWebAdminApi validates HC_INTERNAL_SECRET header on savePageLayout/getPageLayout
    • Security: ProxyGeocode and ProxyReverseGeocode endpoints — geocode SAS tokens and hcazurefunctions7 URL moved server-side
    • Security: GetIpGeoInfo endpoint — IPInfo token moved server-side (removes token from mobile binary)
    • Security: checkConnection no longer bypasses token validation in AppApiHC6
    • Security: GetPortalUploadSas SAS token now bound to specific container and filename
    • Security: HC5 endpoints return generic 500 instead of raw exception messages
    • Security: HC6 portal/app endpoints filter debugMessage from client-facing error responses
    • Security: Firebase credentials fetch has 10-second timeout
    • Fix: HttpsRedirect disabled — was routing to localhost:5000 in production
    • Fix: RunCheckInNotification reads ApiKey from environment variable

1.0.13+11
    • Security: new GetPortalUploadSas endpoint — server-side SAS generation for portal blob uploads (replaces hardcoded tokens)
    • Security: AppApiHC6 catch blocks return generic 500 instead of leaking SP exception details
    • Security: GenericJsonQueryHC6 — parameterised SP calls replacing string-interpolated EXEC in ImportEvents and RecordErrorInDB
    • HcExternalDataIntegration now uses GenericJsonQueryHC6
    • HC5 API files (AppApi, PortalApi, InternalApi, GenericJsonQuery) reverted to pre-audit state — HC5 remains frozen

1.0.12+10
    • Chat (HC6 portal): fire FCM notifications in background — unblocks HTTP response to sender immediately
    • Chat (HC6 portal): parallelize all FCM sends with Task.WhenAll (was sequential per-recipient loop)

1.0.11+9
    • Chat (HC6 app): fire FCM notifications in background — unblocks HTTP response to sender immediately
    • Chat (HC6 app): parallelize all FCM sends with Task.WhenAll (was sequential per-recipient loop)

1.0.10+8
    • markEventChatRead (HC6 app): new endpoint — upserts HC.EventMessageBadgeCounts,
      fans out silent read_sync FCM push to user's other devices

1.0.9+7
    • Chat (HC6): fix MessageReleasabilityFlags deserialization — always-zero due to HC5/HC6 spelling mismatch
    • Chat (HC6): APNS priority 5 + content-available for data-only in-app messages (app + portal shims)
    • Chat (HC6): replace HC5 SP call in DeleteFcmToken with direct UPDATE HC.Device
    • Chat (HC6): detect HC6 error envelope before post-SP switch — errors now surfaced to caller
    • Chat (HC6): replace Console.WriteLine with structured logging in AppApiHC6

1.0.8+6
    • StorePositions: type field limit raised from 54 to 200 chars — enables
      PHO map markers (full blob URL labels are ~165 chars)
    • GetPhotoUploadToken: added runFolder parameter; blob path is now
      trail-photos/{kennelSlug}/{runFolder}/{userId}-{photoGuid}.jpg

1.0.7+5
    • GetPhotoUploadToken: new endpoint — validates auth + kennel membership via
      hcapp_getPhotoUploadToken SP, returns a 15-minute SAS write token for
      kennel-photos/{kennelId}/{userId}-{photoGuid}.jpg
    • GetProfilePhotoUploadToken: new endpoint — validates auth via
      hcapp_getProfilePhotoUploadToken SP, returns a 15-minute SAS write token
      for profile-photos/{userId}.jpg (replaces hardcoded 2028 SAS token)
    • Added Azure.Storage.Blobs 12.22.2 NuGet dependency

1.0.6+4
    • AppApiHC6: exempt checkConnection from deviceId/accessToken guard
      (hcapp_checkConnection is an unauthenticated ping — no auth params required)

1.0.5+3
    • Program.cs: Gzip + Brotli response compression for HC6 endpoints only
      (scoped to /api/AppApiHC6 and /api/PortalApiHC6 via UseWhen — HC5 untouched)

1.0.4+2
    • AppApiHC6: new endpoint at /api/AppApiHC6 — routes to [HC6].[hcapp_...] SPs
      (faithful copy of AppApi; Google Calendar and FCM notification post-processing preserved)

1.0.3+1
    • PublicWebAdminApi: added redeemAdminToken to allowlist for OTP session exchange

1.0.2+1
    • Added PublicWebAdminApi POST endpoint — routes to publicWeb_ SPs via JSON body; allowlisted queryTypes only

1.0.1+2
    • Added 10-second CommandTimeout to PublicWebApi SQL commands to prevent slow SP queries from hanging the HTTP connection

1.0.0+1
    • Added HcExternalDataIntegration timer function — polls HC.Integration every minute, dispatches to registered handlers
    • Added GenericJsonQuery handler — fetches external JSON feeds, reverse-geocodes Google map links, persists to DB
    • Added AzureGeocode — Azure Maps fuzzy-search and reverse-geocode client
    • Added HtmlAgilityPack for HTML parsing in URL expansion
