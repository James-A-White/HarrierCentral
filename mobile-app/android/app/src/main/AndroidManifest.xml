<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Android 12+ requires coarse to be declared alongside fine -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- PackTrack background tracking: geolocator's GeolocatorLocationService
         (foregroundServiceType="location", merged from the plugin manifest)
         pins tracking alive with the "Tracking run in progress" notification
         while the screen is locked or another app is in front. NO
         ACCESS_BACKGROUND_LOCATION — sessions are user-started, while-in-use. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14+: per-type foreground service permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- LocationService enableWakeLock: keep CPU delivering fixes screen-off -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- firebase_messaging: runtime notification permission on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- photo_manager: read device photos (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- photo_manager: write to media store (API < 29) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!-- Package visibility (Android 11+): web links, dialer, share, map apps -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="*/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="Harrier Central"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Large screens (tablets, unfolded foldables) are supported natively:
             FormFactor.preferredOrientations (lib/util/form_factor.dart) lets
             tablet-class devices rotate freely and the layouts handle any
             width, so the Android 16 resizability opt-out
             (PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY) is no longer set.
             android:screenOrientation="portrait" below still applies to phones;
             Flutter overrides it at runtime on tablets. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Theme shown while the Flutter engine initializes -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- image_cropper -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Don't delete: used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>
</manifest>
