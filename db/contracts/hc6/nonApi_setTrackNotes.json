{
  "schema": "HC6",
  "name": "nonApi_setTrackNotes",
  "version": "1.1.0",
  "description": "Fills a hasher's notes on a run from an imported file's title, description, private note and stats line: when the notes are blank, or when the existing notes are exactly the start of the new text (an earlier import's fill, extended by a re-import — 1.1.0, 2026-09-12). Text the hasher wrote or edited is never touched. Called by the track-import processor for every activity that matched a run.",
  "parameters": [
    {
      "name": "eventId",
      "type": "UNIQUEIDENTIFIER",
      "nullable": false,
      "mapsToColumn": "HasherEventMap.EventId",
      "description": ""
    },
    {
      "name": "userId",
      "type": "UNIQUEIDENTIFIER",
      "nullable": false,
      "mapsToColumn": "HasherEventMap.UserId",
      "description": ""
    },
    {
      "name": "notes",
      "type": "NVARCHAR(4000)",
      "nullable": true,
      "mapsToColumn": "HasherEventMap.Notes",
      "description": "Title, blank line, description; trimmed; blank ⇒ no-op."
    }
  ],
  "rowsets": [],
  "sideEffects": [
    "UPDATE HC.HasherEventMap SET Notes WHERE Notes IS NULL or blank — fires the updatedAt trigger."
  ],
  "codeSmells": [],
  "breakingChangeRules": [
    "Never remove a column from any rowset",
    "Never rename a parameter",
    "Never change a parameter type"
  ]
}
