0.21.11
    • New: opt-in 📷 photo showcase on the PackTrack map — playback pauses to zoom each Hash Flash photo out of its pin and back (timed ~3s or tilt-controlled)

0.21.10
    • Added a "Harrier Central 3.0" title banner to the top of the PackTrack live map

0.21.7
    • Fix: past runs not loading on hashruns.org — bucket date format mismatch (ISO string vs YYYY-MM-DD)

0.21.6
    • Security: admin session cookie upgraded to sameSite strict
    • Security: rehype-sanitize added — blocks script/event handler injection in admin-authored markdown
    • Security: HTTP security headers added (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy)
    • Security: OTP redemption rate-limited (10 req/min per IP)
    • Security: X-Internal-Secret header sent to PublicWebAdminApi on save-layout
    • Fix: slug cache now has 10-minute TTL and 500-entry size cap
    • Fix: date format validation on global-runs API route
    • Fix: admin page returns 401 instead of 200 on unauthenticated access

0.21.5
    • Security: admin session HMAC comparison now timing-safe; startup throws if HC_ADMIN_SESSION_SECRET is not set
    • Security: save-layout route no longer returns database error details to the browser

0.21.4
    • Calendar view: day separator line moved above the date header so date and its runs are visually grouped together

0.21.3
    • Kennel logos no longer clipped to any shape across all render locations (StickyNav, RunDetail, RunsPageClient, GlobalRunsList, guest run badge)
    • RichText: raw HTML enabled via rehype-raw — <u>, <mark>, <details>, <sup>, <sub> etc. work in both Rich Text and Content blocks
    • RunListBlock: "Scroll container on desktop" checkbox — limits height to ~one screen, scrolls internally; no effect on mobile

0.21.2
    • Guest run page: runs from other kennels open within the host kennel's theme with attribution and external link
    • Clean URLs on custom domains: londonhash.uk/guest/CityH3/1921 instead of londonhash.uk/lh3/guest/CityH3/1921
    • isCustomDomain flag threaded from middleware header through all page server components into context

0.21.1
    • Fix: RichText colour parameter now applies to headings and bold text, not just body paragraphs
    • Fix: Events page was passing empty run arrays to the renderer — promoted events now display correctly
    • Fix: IsPromotedEvent and EventGeographicScope added to publicWeb_getEvents and publicWeb_getMultiKennelRuns SPs
    • RunListBlock: new "Flat" view type — same layout as Card without background, shadow, or border
    • RunListBlock: "Show divider" option with configurable colour and width between run entries
    • RunListBlock: run name colour and detail text colour properties
    • RunListBlock: "Events only" checkbox now correctly filters on IsPromotedEvent
    • RunCardView: each field on its own line with consistent text sizing; date moved into the detail stack
    • RunCalendarView: time and location split onto separate lines

0.21.0
    • RunListBlock: card/calendar view types; display toggles (number, name, date, time, location, hares, image); kennel logo/name tri-state with size control; multi-kennel slug list with merged sorted results; max runs/days filters; calendar empty-day rows; kennel logo column in calendar
    • NextRunBlock: run offset (−5 to +10), hide-when-no-run / placeholder text, display toggles including description, tags, map
    • Rich Text Block: new block for markdown prose — inline links, headings, lists, bold, italic, blockquote
    • ContentBlock: body upgraded to markdown
    • FeaturedRunCard: embedded map, tag pills, per-field display options
    • RowBlock: migrated from DropZone to Puck slot fields; responsive collapse-below breakpoint
    • middleware.ts → proxy.ts (Next.js 16 deprecation fix)
    • publicWeb_getMultiKennelRuns SP: cross-kennel run query with kennel branding per row
    • LH3 About page assembled from Rich Text and Content blocks

0.20.0
    • Row block: 2–4 columns with per-column flex widths (1–10), gap, and vertical alignment controls; blocks can be dropped into each column
    • All blocks: background colour and padding controls added universally
    • About block removed — superseded by Content block

0.19.0
    • ContentBlock replaces ImageTextBlock — separate heading/body colour and alignment controls; heading/body gap cycling button (—/S/M/L/XL)
    • ContentBlock button: visual 2×3 position grid, page picker, text/colour/alignment, 20-icon visual picker (songs/runs/events/stats + common), icon position
    • Padding cross controls replace all padding dropdowns — compact ↑←→↓ cycling widget for block, image, text, and button padding zones
    • Portal: Welcome Text field removed from Kennel Website content tab

0.18.0
    • Page management panel in Puck editor — Globe (active/draft) and Eye (menu visibility) controls per page
    • Drag-to-reorder pages; default pages bolded; draft pages greyed out
    • Custom pages: add with auto-generated slug (validated), delete; served at /[slug]/[pageSlug]
    • Draft custom pages return 404; url-only pages accessible by URL but not shown in nav
    • ButtonBlock: link button with target page picker, style (primary/outline/ghost), alignment, padding
    • WelcomeBlock removed from block picker
    • ImageTextBlock: border toggle, border colour, border width, and corner radius controls
    • StickyNav reads page visibility from SiteConfig — designer controls the menu, not portal settings
    • SiteConfig replaces PageLayoutBlob — layout, visibility, and order unified in one JSON blob

0.17.1
    • Image upload in Puck ImageTextBlock — uploads to Azure Blob Storage via admin-authenticated API route
    • Layout dropdown: side-by-side ratios (10–90%), image only, text only, image above text, text above image
    • Top/bottom padding presets (None/Small/Medium/Large/XL) and left/right padding (0–30%) on ImageTextBlock
    • Block background colour and text colour dropdowns (theme tokens) on ImageTextBlock
    • Puck editor canvas shows kennel background (image + blur + colour overlay) for accurate colour preview
    • Kennel CSS theme variables active in editor so var(--kennel-*) tokens render correctly

0.17.0
    • Multi-page Puck editor — Home, Runs, About, Events, Songs, Stats all editable per kennel
    • Page dropdown in editor header; unsaved-changes guard (Publish & Switch / Discard) when switching
    • Publish toast feedback (green = success, red = failure)
    • PageLayoutBlob structure: all 6 page layouts stored in single PageLayoutJson blob, keyed by page type
    • 5 new blocks: RunsPageBlock, AboutBlock, EventsListBlock, SongsListBlock, StatsListBlock
    • All 5 sub-pages now render via PuckRenderer using saved or default layout

0.16.0
    • Kennel landing pages now load layout from the DB (PageLayoutJson), falling back to default when none is saved
    • Puck page builder blocks: Welcome, Next Run, Run List — kennel data flows server-side via React context
    • Fixed shell (StickyNav, ScrollHero, SiteFooter) always present, not editable via page builder
    • Editor available at /lh3/admin/layout (locked to LH3 until OTP auth is implemented)
    • Original page preserved at /lh3/legacy as a safety net

0.15.1
    • Run times now show kennel-local timezone (e.g. 19:15 JST)
    • Detail panel shows viewer's local time below when it differs (e.g. 11:15 BST your time)
    • Timezone abbreviations derived from long English name when short form returns GMT+X offset
    • SPs return EventStartDatetimeGmt and KennelIANATimezone with on-the-fly fallback for older runs

0.15.0
    • Three-button colour system applied site-wide: --kennel-btn-primary, --kennel-btn-secondary, --kennel-btn-cancel
    • Accent colour for run number badge surfaces and detail icons
    • Hero Image renders in place of kennel logo in hero; OG priority: hero → welcome image → logo
    • Removed gradient tint from Next Run banner in sticky nav

0.14.1
    • Timezone-aware run boundaries; sticky nav and version display fixes

0.14.0
    • Unified run detail component and redesigned layout

0.13.3
    • Run card: kennel logo and name hidden on single-kennel runs page — showKennelBranding prop controls this; multi-kennel lists retain branding

0.13.2
    • Card border now fully visible: dark:white/60, light:zinc-600 — border always renders above run image via overflow-hidden
    • Hairline opacity increased: dark:white/30, light:zinc-400
    • Kennel logo vertically centred within card body

0.13.1
    • Run images restored — appear between hairline and body section, full width, natural aspect ratio
    • Card background: dark:bg-black/30 (darkens by 30%) / bg-white/30 (lightens by 30%)

0.13.0
    • Run card redesigned: event name header + hairline, then kennel logo + text column
    • Text column shows: kennel name (primary colour), run number with relative time, date/time, address, location description, event type
    • relativeTime helper: "today", "tomorrow", "in N days", "N days ago"
    • Selected card: primary-colour border + subtle primary tint on title bar

0.12.9
    • Wix embed side margins trimmed from 40px to 24px

0.12.8
    • Wix embed: panel height now computed from window.innerHeight − 80px, updated on resize
    • html/body overflow locked to hidden when embedded — prevents Wix auto-resize from expanding iframe
    • Sticky header in embedded mode: search bar, Future/Past tabs, version number stay fixed; only card list scrolls
    • Outer container clipped with overflow-hidden so Wix designers can see true content boundary
    • Version number moved below Future/Past tabs when embedded (always visible for testing)

0.12.7
    • Fix runs page on iOS mobile: cards below the first card are now clickable in Wix embed and direct browser
    • Removed overflow-x:hidden + touch-pan-y from the run list container — combination caused iOS to restrict tap targets
    • Simplified panel height to CSS dvh — removes window.innerHeight snapshot that produced wrong height at mount time in Wix iframes
    • Back button now restores selected run and card highlight on mobile
    • Run card highlights immediately on tap before navigating to detail
    • No stagger animation when returning via back button (instant card restore)

0.12.6
    • Legacy hash URLs now redirect to a real server-side page (/rd/<uuid>) — no overlay, no flash, back button works correctly
    • /rd/[uuid] resolves the kennel UUID server-side and renders the runs page identically to /<slug>/runs
    • LegacyRedirectHandler stripped to multi-kennel picker only — single UUID handled by inline script before first paint
    • Deleted /api/legacy-runs route (no longer needed)

0.12.5
    • Eliminate global runs flash on legacy hash URLs — inline script hides global page before first paint
    • Global page revealed immediately if hash is not a legacy URL or kennel resolution fails

0.12.4
    • Version strip added to bottom of runs page — fixed 20px bar, always visible, reads from package.json at build time

0.12.3
    • Legacy hash URLs now render the kennel runs page inline — no redirect, no navigation
    • Handles both #/RD?publicKennelIds=<uuid> (query string) and #/RD/<uuid> (path) formats
    • New /api/legacy-runs route — fetches kennel context + runs in a single server-side call
    • Extracted toKennelContext and helpers to lib/kennel-utils.ts — shared by all kennel pages

0.12.2
    • Fix /<slug>/runs server error — past events call was missing required boundary parameter (daysOffset: 3650)
    • Legacy redirect shim now points to /<slug>/runs to match old hashruns.org run list behaviour

0.12.1
    • Legacy URL redirect shim for old hashruns.org /#/RD?publicKennelIds=<uuid> links
    • New SP publicWeb_resolveKennelsByUuid — resolves kennel UUIDs to slugs and custom domains
    • New /api/resolve-kennels route — server-side UUID lookup called by the shim
    • Updated CLAUDE.md files to document path-based URL scheme and mark old format deprecated

0.7.1
    • Global runs page: background image fills entire page including behind the run list
    • Run list left panel: transparent background, all 4 card corners now visible
    • Tab switcher: redesigned as segmented control pill (Future Runs / Past Runs)
    • Kennel logos: no longer clipped to circles — display at natural shape with rounded corners
    • Card typography: larger fonts throughout (event name/kennel name 24px, details 20px)
    • Kennel logo size increased (64px → 112px)
    • Left panel widened to 560px on desktop
    • Card vertical padding significantly reduced
    • Selected card indicator: 3× wider (9px), absolutely positioned so content does not shift
    • Font: Avenir Next applied globally via --font-sans CSS variable

0.7.0
    • ScrollBlur component on kennel landing pages
    • KennelContext rename (toMockKennel → toKennelContext, MockKennel → KennelContext)
    • Removed FILTH fallback join — kennel styles now sourced exclusively from HC.KennelWebsite
    • Logo and favicon now sourced from HC.Kennel (canonical source)
    • Theme colours read from DB at runtime

0.6.0
    • Global calendar page
    • Nav updates
    • Fee display fix
