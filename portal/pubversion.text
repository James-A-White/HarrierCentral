
1.5.0+624
    • Chat — service worker: guard data-only payloads in onBackgroundMessage —
      null dereference on payload.notification.title was crashing the SW when
      in-app-only (data-only) push messages arrived while the tab was unfocused
    • Chat — portal FCM listener: try/catch + null-safe payload parsing +
      catchError on _refreshMessages so a malformed payload can't silently
      kill live updates for the session
    • Run list — badge FCM listener: same defensive hardening as chat listener

1.4.13+623
    • Chat — fix double-tick race: await chatController.updateMessage so message
      reaches 'sent' status before FCM echo arrives and upgrades it to 'delivered'
    • Chat — FCM subscription now always registered even if initial message load
      throws an exception (was silently lost on load error)
    • Chat — portal now queues a pending refresh when FCM arrives during an
      in-progress fetch, matching mobile behaviour (was silently dropped)

1.4.12+622
    • Chat — delivery receipts: single tick on server confirm, double tick
      when FCM echo arrives back at the sender
    • Chat — DEBUG: dialog after each send shows full-push and in-app-only
      recipients by name with per-user device count

1.4.11+621
    • Platform Admin tab in kennel editor — platform admins (CanEditKennel) can
      now set kennel operational status (Active / Inactive-Visible /
      Inactive-Hidden / Defunct)
    • Replaced hardcoded platform admin IDs with HC.PlatformAdmin DB table
    • Kennel status dropdown labels updated to reflect new four-way split

1.4.10+620
    • Chat — FCM now triggers a delta-fetch from the DB (sinceSequenceCount)
      rather than inserting directly from the FCM payload

1.4.9+619
    • Fix location lookup dialog crash — pass controller as constructor param
      instead of Get.find() to avoid "not found" error during close animation

1.4.8+618
    • RunLocationLookupDialog converted to GetX — controller manages all
      state, dialog is StatelessWidget, map sub-widgets use ever() listeners
      for reactive marker repaints
    • _buildPin extracted as shared function, eliminating duplicated marker
      rendering between Previous Runs and Gazetteer map tabs

1.4.7+617
    • Fix memory leak: searchFocusNode and searchController moved from
      module-level globals into RunListPageController, disposed in onClose()
    • Fix stale run list: editing a run now refreshes the sidebar list
      concurrently with the detail panel re-fetch
    • RegisterVirginPage converted from StatefulWidget to StatelessWidget
    • RunDetailWidget converted from StatefulWidget to StatelessWidget

1.4.5+615
    • Location lookup: previous runs with no event-level lat/lon no longer appear in the list
    • SP fix: syncLat/syncLong returns NULL (not city fallback) for unlocated events

1.4.4+614
    • Wire up "Is Promoted Event" checkbox — now correctly sent to hcportal_addEditEvent
    • Add "Event Type" (Geographic Scope) dropdown to run edit Misc tab with values matching mobile app

1.4.3+613
    • Update Flutter SDK constraint to ^3.41.9 and Dart SDK to >=3.11.5

1.4.2+612
    • Fixed check-in sheet hanging indefinitely on kennels with slow or unreachable logo URLs
    • Added 10s timeout + graceful fallback to logo fetch; errors now logged instead of silently swallowed

1.4.1+611
    • Added "Design Website" button to kennel app bar and detail panel
    • Tapping generates a one-time token via hcportal_generateWebAdminToken and opens the page builder in the browser
    • Token expires in 10 minutes; no password required

1.4.0+610
    • Three-button colour system: primary, cancel, secondary (replaces single button colour)
    • Hero Image field: replaces kennel logo in hero when set; accepts PNG/AVIF for transparency; used as primary OG/social share image
    • Content tab: shows Tagline + Welcome Text only; fixed welcome text field not rendering

1.3.8+609
    • New Kennel Website editor (tabbed UI): identity, appearance, images, content, SEO, advanced tabs
    • EditableColorField widget with colour picker dialog
    • Edit Website button on run list page

1.3.7+608
    • Removed circular clip and border from kennel logo strip — logos now display at their natural shape
    • Flutter 3.41.2 → 3.41.6
    • 35 pub dependency updates (Firebase, build_runner, badges, trina_grid, pdf, printing, etc.)

1.3.6+607
    • Added Newsflash feature — admin management page, startup dialog, 6 HC6 SPs and two new DB tables (HC.PortalNewsflash, HC.PortalNewsflashRead)
    • Added HC Admin Tools hub page replacing the Monitor nav button
    • Fixed BIT column JSON parsing (SQL Server BIT serialises as bool, not int)
    • AVIF image support via HcNetworkImage in newsflash dialog and management page
    • Updated portal deploy process to Azure Static Web Apps

1.3.5+606
    • Relabelled "Can edit run attendance" to "User can set run attendance"

1.3.4+605
    • Location Lookup: dialog now auto-searches on open using the place description value
    • Auto-selects when exactly 1 past run matches; filters list for multiple matches; falls through to gazetteer search when no match
    • Gazetteer: "Searching…" text while loading, better spinner contrast, auto-selects single result

1.3.3+604
    • Add "Can Edit Run Attendance" dropdown to run editor Misc tab
    • Make Kennel admin email list optional
    • Fix: stale device credentials no longer cause a permanent auth failure on login

1.3.2+603
    • Kennel admin email field made editable (was read-only)

1.0.0+563
    • Major refactor
    • Check for all unawaited Futures
    • Replaced discontinued pluto grid
    • Other significant cleanups